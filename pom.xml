<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <artifactId>cpf-cnpj-utils</artifactId>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <configuration>
          <encoding>${project.encoding}</encoding>
        </configuration>
        <version>${maven.resource.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <configuration>
          <autoPublish>false</autoPublish>
          <publishingServerId>central</publishingServerId>
        </configuration>
        <extensions>true</extensions>
        <groupId>org.sonatype.central</groupId>
        <version>${central.publishing.maven.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>
            --add-opens cpf.cnpj.utils/io.github.felseje.cpf=org.junit.platform.commons
            --add-opens cpf.cnpj.utils/io.github.felseje.cpf.exception=org.junit.platform.commons
            --add-opens cpf.cnpj.utils/io.github.felseje.cnpj=org.junit.platform.commons
            --add-opens cpf.cnpj.utils/io.github.felseje.cnpj.exception=org.junit.platform.commons
            --add-opens cpf.cnpj.utils/io.github.felseje.exception=org.junit.platform.commons
          </argLine>
          <failIfNoTests>true</failIfNoTests>
          <useModulePath>true</useModulePath>
        </configuration>
        <groupId>org.apache.maven.plugins</groupId>
        <version>${maven.surefire.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
            <id>attach-sources</id>
          </execution>
        </executions>
        <groupId>org.apache.maven.plugins</groupId>
        <version>${maven.source.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-javadoc-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>jar</goal>
            </goals>
            <id>attach-javadocs</id>
          </execution>
        </executions>
        <groupId>org.apache.maven.plugins</groupId>
        <version>${maven.javadoc.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-gpg-plugin</artifactId>
        <configuration>
          <gpgArguments>
            <arg>--pinentry-mode</arg>
            <arg>loopback</arg>
          </gpgArguments>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>sign</goal>
            </goals>
            <id>sign-artifacts</id>
            <phase>verify</phase>
          </execution>
        </executions>
        <groupId>org.apache.maven.plugins</groupId>
        <version>${maven.gpg.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <encoding>${project.encoding}</encoding>
          <release>${java.version}</release>
          <source>${java.version}</source>
          <target>${java.version}</target>
        </configuration>
        <groupId>org.apache.maven.plugins</groupId>
        <version>${maven.compiler.plugin.version}</version>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <artifactId>junit-jupiter</artifactId>
      <groupId>org.junit.jupiter</groupId>
      <scope>test</scope>
      <version>${junit.version}</version>
    </dependency>
    <dependency>
      <artifactId>mockito-core</artifactId>
      <groupId>org.mockito</groupId>
      <scope>test</scope>
      <version>${mockito.version}</version>
    </dependency>
    <dependency>
      <artifactId>mockito-junit-jupiter</artifactId>
      <groupId>org.mockito</groupId>
      <scope>test</scope>
      <version>${mockito.version}</version>
    </dependency>
  </dependencies>
  <description>
    A minimal Java library to generate and validate Brazilian CPF and CNPJ
    (supporting both numeric and alphanumeric formats). Lightweight and dependency-free.
    Keywords: cpf, cnpj, validation, brazil, alphanumeric, document, utils.
  </description>
  <developers>
    <developer>
      <email>felseje@hotmail.com</email>
      <id>felseje</id>
      <name>Felseje</name>
      <url>https://github.com/felseje</url>
    </developer>
  </developers>
  <groupId>io.github.felseje</groupId>
  <licenses>
    <license>
      <distribution>repo</distribution>
      <name>BSD 3-Clause License</name>
      <url>https://opensource.org/licenses/BSD-3-Clause</url>
    </license>
  </licenses>
  <modelVersion>4.0.0</modelVersion>
  <name>CpfCnpjUtils</name>
  <organization>
    <name>Felseje</name>
    <url>https://github.com/felseje</url>
  </organization>
  <packaging>jar</packaging>
  <properties>
    <central.publishing.maven.plugin.version>0.7.0</central.publishing.maven.plugin.version>
    <java.version>17</java.version>
    <junit.platform.version>1.12.2</junit.platform.version>
    <junit.version>5.12.2</junit.version>
    <maven.compiler.plugin.version>3.11.0</maven.compiler.plugin.version>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <maven.gpg.plugin.version>3.2.7</maven.gpg.plugin.version>
    <maven.javadoc.plugin.version>3.11.2</maven.javadoc.plugin.version>
    <maven.resource.plugin.version>3.3.1</maven.resource.plugin.version>
    <maven.source.plugin.version>3.3.1</maven.source.plugin.version>
    <maven.surefire.plugin.version>3.5.3</maven.surefire.plugin.version>
    <mockito.version>5.17.0</mockito.version>
    <project.encoding>UTF-8</project.encoding>
  </properties>
  <scm>
    <connection>scm:git:https://github.com/felseje/cpf-cnpj-utils.git</connection>
    <developerConnection>scm:git:git@github.com:felseje/cpf-cnpj-utils.git</developerConnection>
    <tag>HEAD</tag>
    <url>https://github.com/felseje/cpf-cnpj-utils</url>
  </scm>
  <url>https://github.com/felseje/cpf-cnpj-utils</url>
  <version>1.0.0-alpha</version>
</project>